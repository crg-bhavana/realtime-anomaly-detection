project_name: realtime-anomaly-detection
kafka:
  bootstrap_servers: "localhost:9092"
  topic: "ops-metrics"
features:
  - request_count
  - error_rate
  - avg_latency_ms
  - cpu_utilization
  - memory_utilization
model:
  contamination: 0.05
  random_state: 42
